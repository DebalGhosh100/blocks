base-path: /usr/local/bin
python-version: python3.10

commands:
  install: ${test.base-path}/${test.python-version} -m pip install
  run: ${test.base-path}/${test.python-version} script.py

servers:
  - name: server1
    command: ${test.commands.install}
  - name: server2
    command: ${test.commands.run}
