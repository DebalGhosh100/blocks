blocks:
  # Simulate your workflow to test directory persistence
  
  # Start in root (simulated)
  - run: "mkdir -p poky"
  - run: "cd poky"
  - run: "pwd"
  
  # Simulate the source command (creates a 'build' directory like oe-init-build-env does)
  - run: "mkdir -p build && cd build"
  - run: "pwd"
  
  # Line 15 equivalent: Run command then cd back up
  - run: "mkdir -p test_layer && echo 'layer added' && cd .."
  - run: "pwd"  # Should be back in poky/
  
  # Create version directories (like lines 17-18)
  - run: "mkdir -p yocto_5.0.13/results"
  - run: "mkdir -p yocto_5.0.12/results"
  
  # Line 20 equivalent: cd into first results directory and do something
  - run: "cd ./yocto_5.0.13/results && echo 'downloaded file 13' > file13.txt"
  - run: "pwd"  # Should be in poky/yocto_5.0.13/results
  
  # Line 22 equivalent: cd into second results directory
  # This is the PROBLEMATIC line - it uses relative path ./yocto_5.0.12/results
  # but we're currently in poky/yocto_5.0.13/results, so this path doesn't exist!
  - run: "cd ./yocto_5.0.12/results && echo 'downloaded file 12' > file12.txt"
  - run: "pwd"  # Where are we?
  
  # Cleanup
  - run: "cd ../../.."  # Go back to root
  - run: "rm -rf poky"
